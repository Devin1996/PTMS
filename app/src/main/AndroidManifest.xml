<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ptms">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReadConfirmedBusBooking"></activity>
        <activity android:name=".ChatBotActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".SwipeViewActivity" />
        <activity android:name=".SpinnerTestActivity" />
        <activity android:name=".Search4BookActivity" />
        <activity android:name=".ReportBusActivity" />
        <activity android:name=".ReviewBusActivity" />
        <activity android:name=".AskHelpActivity" />
        <activity android:name=".OnBoardActivity" />
        <activity android:name=".BusReportActivity" />
        <activity android:name=".ScanQrActivity" />
        <activity android:name=".ConfirmFinalBooking" />
        <activity android:name=".MyTravelPlansActivity" />
        <activity android:name=".DateTimeActivity" />
        <activity android:name=".AddToTravelPlansActivity" />
        <activity android:name=".TimeDisplayActivity" />
        <activity android:name=".TimeSchedulesActivity" />
<!--        <activity-->
<!--            android:name=".TimeScheduleActivity"-->
<!--            android:label="@string/title_activity_time_schedule"-->
<!--            android:theme="@style/AppTheme.NoActionBar" />-->
        <activity android:name=".PassSettingsActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".PasMenuActivity"
            android:label="@string/title_activity_pas_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PasLoginActivity" />
        <activity android:name=".PasRegActivity" />
        <activity android:name=".PasMainActivity" />
        <activity android:name=".PassengerSettingsActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".PassengerMapActivity"
            android:label="@string/title_activity_passenger_map" />
        <activity
            android:name=".ConductorMapActivity"
            android:label="@string/title_activity_conductor_map" />
        <activity
            android:name=".DriversMapsActivity"
            android:label="@string/title_activity_drivers_maps" />
        <activity android:name=".ConductorLogin" />
        <activity android:name=".DriverLogin" />
        <activity android:name=".TestActivity" />
        <activity android:name=".PassengerLogin" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:authorities="${applicationId}.here.this.library.provider"
            android:name="androidx.core.content.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities"
            >

        </provider>
    </application>

</manifest>